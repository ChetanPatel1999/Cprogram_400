// wap to check two given number are same or differnt.
#include <stdio.h>
void main()
{
    int num1, num2;
    printf("enter num1 : ");
    scanf("%d", &num1);
    printf("enter num2 : ");
    scanf("%d", &num2);
    num1 == num2 ? printf("both num are same") : printf("both num are different");
}