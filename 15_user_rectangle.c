// wap to find area of reactangle.
#include <stdio.h>
void main()
{
    float width, length, area;
    printf("given length : ");
    scanf("%f", &length);
    printf("given width : ");
    scanf("%f", &width);
    area = length * width;
    printf("area of reactangle = %.2f", area);
}
